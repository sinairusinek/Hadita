# Images (large, not suitable for git) — except bundled app images
*.jpg
!images/*.jpg
!000nvrj-432316TAX 1-85_page-0010.jpg
!000nvrj-432316TAX 1-85_page-0050.jpg
# Talk figures are the published site's content, so they must be tracked.
!talk-vienna-2026/img/*.jpg

# ML models
*.mlmodel

# Python
__pycache__/
*.pyc

# OCR cache (generated)
.ocr_cache/

# OS
.DS_Store

# Claude
.claude/
*.jpeg
*.jpeg
*.html
*.bak
*.bak.*
kraken_*.csv

# Generated viewer datasets (symlinks into the corpus)
tools/pagexml-viewer/*/

# Downloaded model weights (7GB, re-fetchable; exceeds GitHub's 100MB file cap)
models/

# dinglehopper virtualenv (382MB, re-creatable via pip install dinglehopper)
.venv-dinglehopper/
# CERatosaurus upload bundles (regenerated from exp2608 XMLs on demand)
cerato_upload/
